apiVersion: v1
kind: Service
metadata:
  name: {{ include "cost-report.fullname" . }}
  labels:
    {{- include "cost-report.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "cost-report.selectorLabels" . | nindent 4 }}
